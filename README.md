#Web crawler based on Python3
#The idea is to extract all the URL to join the queue, to download all the URL cache to prevent duplication of download, the URL address into the relative address of the disk

#基于Python3 的网站抓取工具
#思路就是提取所有的URL加入到队列，把所有下载过得URL缓存防止重复下载，把URL地址转换为磁盘下的相对地址

